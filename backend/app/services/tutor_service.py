def generate_tutor_response(query: str, context_chunks: list[str], topic: str) -> str:
    """STUB — replace with real RAG + local LLM (Ollama) later."""
    return f"[stubbed response] Would answer '{query}' about '{topic}' using {len(context_chunks)} retrieved chunks."
