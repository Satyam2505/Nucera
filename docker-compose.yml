services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: nucera
    ports:
      - "5432:5432"
    volumes:
      - nucera_pgdata:/var/lib/postgresql/data

volumes:
  nucera_pgdata:
