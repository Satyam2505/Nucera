/** @type {import('next').NextConfig} */
const nextConfig = {
  // The account menu now lives in the same bottom-left corner as Next's
  // dev-mode build indicator — turn the indicator off so they don't overlap.
  devIndicators: false,
};

module.exports = nextConfig;
